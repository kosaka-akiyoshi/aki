<?php

    function kagitoridashi(array $array): int
    for($count = 0; $count < 1000; $count++)
    {

        $new = key($data);
        $tugi = next($data);
        echo($new);

    };
    
    function rensoukagitoridashi(array $array): string
    for($count = 0; $count < 1000; $count++)
    {
        $new = key($data);
        $tugi = next($data);
        echo($new);

    };
    
?>